package com.spherixlabs.trekscape.home.domain.enums

/**
 * [HomeType] is an enum that represents the navigation screen type of the home screen.
 * */
enum class HomeType {
    HISTORY,
    RECOMMENDATIONS,
    PROFILE
}